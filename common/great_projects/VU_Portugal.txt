#Palácio Nacional de Mafra
vu_palacio_nacional_de_mafra = {
	#province it starts in
	start = 8098

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1717.01.01 #Palace

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1717
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_palacio_nacional_de_mafra
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_palacio_nacional_de_mafra
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1730 #First Upgrade and last upgrade took place between 1771 and 1794
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 156
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    allowed_num_of_buildings = 1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
			local_tax_modifier = 0.05
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    heir_chance = 0.25
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 276
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    allowed_num_of_buildings = 2
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
			local_tax_modifier = 0.075
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    heir_chance = 0.35
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 120
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    allowed_num_of_buildings = 3
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
			local_tax_modifier = 0.10
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    heir_chance = 0.5
		    governing_capacity = 25
		    prestige = 0.5
		   	legitimacy = 5
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.1
				}
			}
		}
	}
}

#Mosteiro da Batalha
vu_mosteiro_da_batalha = {
	#province it starts in
	start = 8098

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1386.01.01 #Monastery

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1386
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_mosteiro_da_batalha
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_mosteiro_da_batalha
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1517 #First Upgrade and last upgrade took place between 1771 and 1794
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 1572
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    prestige_from_land = 0.15
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 480
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.15
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
			local_unrest = -1
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    prestige_from_land = 0.175
			prestige = 0.35
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 240
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.20
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
			local_unrest = -1
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    prestige_from_land = 0.2
			prestige = 0.5
			establish_order_cost = -0.15
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.1
				}
			}
		}
	}
}

#Mosteiro dos Jerónimos
vu_mosteiro_dos_jeronimos = {
	#province it starts in
	start = 227

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1502.01.01 #Monastery

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1502
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_mosteiro_dos_jeronimos
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_mosteiro_dos_jeronimos
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1602
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 1200
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.05
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    colonist_placement_chance = 0.05
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 480
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.075
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    colonist_placement_chance = 0.075
			range = 0.15
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 240
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    colonist_placement_chance = 0.1
			range = 0.175
			colonists = 1
			merchants = 1
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
			owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.1
				}
			}
		}
	}
}

#Aqueduto das Águas Livres
vu_aqueduto_das_aguas_livres = {
	#province it starts in
	start = 227

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1731.01.01 #Aqueduct

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1731
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_aqueduto_das_aguas_livres
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_aqueduto_das_aguas_livres
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1748
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 204
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_development_cost = -0.1
			trade_goods_size = 1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 102
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_development_cost = -0.2
			trade_goods_size = 2
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    technology_cost = -0.5
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 51
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_development_cost = -0.3
			trade_goods_size = 3
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    technology_cost = -0.1
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		}
	}
}

#Castelo dos Mouros
vu_castelo_dos_mouros = {
	#province it starts in
	start = 3776

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1147.01.01 #Castle

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1147
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_castelo_dos_mouros
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_castelo_dos_mouros
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1383
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 240
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 480
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.15
			local_attacker_dice_roll_bonus = -1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 960
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.15
			local_attacker_dice_roll_bonus = -1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    defensiveness = 0.15
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.1
				}
			}
		}
	}
}

#Linhas de Torres Vedras
vu_linhas_de_torres_vedras = {
	#province it starts in
	start = 8098

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1809.01.01 #Castle

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1809
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_linhas_de_torres_vedras
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_linhas_de_torres_vedras
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1812
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 240
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.15
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 480
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.30
			local_attacker_dice_roll_bonus = -1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 960
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.50
			local_attacker_dice_roll_bonus = -2
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.1
				}
			}
		}
	}
}

#Igreja e Torre dos Clérigos
vu_igreja_e_torre_dos_clerigos = {
	#province it starts in
	start = 231

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1732.01.01 #Church

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1732
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture_group = iberian
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_igreja_e_torre_dos_clerigos
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_igreja_e_torre_dos_clerigos
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture_group = iberian
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1750
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture_group = iberian
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 336
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.1
			local_unrest = -1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 168
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.2
			local_unrest = -2
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    global_missionary_strength = 0.1
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.1
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 84
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_tax_modifier = 0.3
			local_unrest = -3
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    global_missionary_strength = 0.1
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.15
				}
			}
		}
	}
}

#Castelo de Guimarães
vu_castelo_de_guimaraes = {
	#province it starts in
	start = 3104

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1000.01.01 #Castle

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1200
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_castelo_de_guimaraes
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_castelo_de_guimaraes
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1200
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 240
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 480
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.15
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    army_tradition = 0.25
			prestige = 0.25
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 960
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_defensiveness = 0.2
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    army_tradition = 0.5
			prestige = 0.5
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_nobles
					loyalty = 0.1
				}
			}
		}
	}
}

#Mosteiro de Alcobaça
vu_mosteiro_de_alcobaca = {
	#province it starts in
	start = 8098

	#date built in real life (so anything built during game time will be there if you start a game after that date)
	date = 1178.01.01 #Church

	#time to build
	time = {
		months = 0
	}

	#how much to build one
	build_cost = 0

	#can we move it?
	can_be_moved = no

	#time to move the project one unit of distance, in days
	move_days_per_unit_distance = 1

	#tier that the project starts at when first placed in the game (be that at game start or when built by a country as the game progresses)
	starting_tier = 1

	#project type
	type = monument

	#can we build it?
	build_trigger = {
	    is_year = 1178
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#what to do when it's built
	on_built = {
		show_ambient_object = vu_mosteiro_de_alcobaca
	}

	#what to do when it's destroyed
	on_destroyed = {
		hide_ambient_object = vu_mosteiro_de_alcobaca
	}

	#can our country use it?
	can_use_modifiers_trigger = {
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country upgrade it?
	can_upgrade_trigger = {
	    is_year = 1252
		OR = {
			AND = {
				religion_group = christian
				has_owner_religion = yes
			}
			AND = {
				culture = portugese
				OR = {
					has_owner_culture = yes
					has_owner_accepted_culture = yes
				}
			}
		}
	}

	#can our country keep it or is it destroyed when we get hold of it?
	keep_trigger = {
	}

	#tier data
	tier_0 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 0
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 0
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		}

		#what effects happen when this tier is achieved
		on_upgraded = {

		}
	}

	tier_1 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 888
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 3500
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_unrest = -1
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    global_missionary_strength = 0.1
			religious_unity = 0.05
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.05
				}
			}
		}
	}
	
	tier_2 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 444
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 7000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_unrest = -2
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    global_missionary_strength = 0.2
			missionaries = 1
			religious_unity = 0.1
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.075
				}
			}
		}
	}

	tier_3 = {
		#time to upgrade to this level (0 for tier 0)
		upgrade_time = {
			months = 222
		}

		#cost to upgrade to this level (0 for tier 0)
		cost_to_upgrade = {
			factor = 14000
		}

		#what modifiers are added to the province when we have this project here on this tier
		province_modifiers = {
		    local_unrest = -3
		}

		#what modifiers are added to the provinces in the map area when we have this project here on this tier
		area_modifier = {
		}

		#what modifiers are added to the country when we have this project here on this tier
		country_modifiers = {
		    global_missionary_strength = 0.3
			missionaries = 2
			religious_unity = 0.15
		}

		#what effects happen when this tier is achieved
		on_upgraded = {
		    owner = {
				add_estate_loyalty = {
					estate = estate_church
					loyalty = 0.1
				}
			}
		}
	}
}